#!/usr/bin/env sh
set -eu

echo "[pre-push] Running full repository quality checks..."
make quality

echo "[pre-push] Running repository test gate..."
uv run pytest tests/ -x -q --ignore=tests/integration/video

echo "[pre-push] Quality checks passed."
